#include <stdio.h>

int main()
{
    printf("Bien le bonjour !");
    int bricoleur = 3;
    if (bricoleur != 2)
    {
        printf("pas ouf du ocup");
    }

    printf('yo les gars');
    int test = 35;

    if (test > 32)
    {
        printf('salut');
    }
}